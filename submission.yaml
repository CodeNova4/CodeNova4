# =============================================================================
# HACKATHON SUBMISSION METADATA
# =============================================================================
# Instructions:
#   - Fill in ALL required fields (marked with # REQUIRED)
#   - Optional fields can be left as empty strings ""
#   - Do NOT rename this file — the evaluation pipeline reads it by name
#   - Use double quotes around all string values
# =============================================================================

team:
   name: "CodeNova4"                             # REQUIRED — e.g., "Orion Squad"
   track: "AI"                            # REQUIRED — one of: AI | DevOps | Sustainability | Open
  lead:
    name: "Aarohi Patel"                           # REQUIRED — e.g., "Alice Chen"
    email: "aarohipatel2507@gmail.com"            # REQUIRED — e.g., "alice.chen@ibm.com"
  members:
    - name: "Aastha Patel"                         # REQUIRED — Team member 1
      email: "aasthap309@gmail.com"
    - name: "Krisha Patel"                         # Add more members by copying the two lines above
      email: "krishaapatel112211@gmail.com"
    - name: "Purva Patel"
      email: "purvapatel5509@gmail.com"

submission:
  title: "Bob — Mission Readiness & Predictive Maintenance Copilot"                            # REQUIRED — Project display name, e.g., "SmartOps Dashboard"

  problem_statement: >                 # REQUIRED — 1-3 sentences. What problem? Who has it?
    "Military organisations cannot reliably determine whether aircraft, vehicles,and equipment are mission-ready. Maintenance runs on fixed calendar schedules
    regardless of actual component condition, and HUMS (Health & Usage Monitoring System) sensor data that could predict failures weeks in advance sits unanalysed —
    so platforms fail unexpectedly, readiness drops, and recovery takes weeks."



  solution_summary: >                  # REQUIRED — 1-3 sentences. What did you build?
    "A Bob Copilot that ingests HUMS sensor data and service records, flags which assets are not mission-ready, explains each readiness issue in plain language, predicts which components will breach failure thresholds before
    the next mission window, and produces one fleet-wide prioritized maintenance plan ranked by urgency."


  key_features:  
    - "Fleet-wide readiness snapshot — Mission Ready / At Risk / Non-Mission-Capable counts at a glance"
    - "Automated non-ready asset detection against per-component sensor thresholds"
    - "Plain-language issue explanations for every flagged component, generated via IBM watsonx.ai (Granite) with a rule-based fallback"
    - "Predictive failure timing — projects days-to-breach per component and checks it against the next mission window"
    - "Fleet-wide prioritized maintenance plan ranking every flagged component by urgency with a recommended action"


  tech_stack:
     languages: ["JavaScript (JSX)", "HTML5", "CSS3"]
     frameworks: ["React 18", "Babel Standalone", "Recharts"]
     ibm_technologies: ["watsonx.ai (Granite model)", "IBM Db2 / Cloudant", "IBM Cloud IAM"]
     databases: ["IBM Db2 / Cloudant (live mode)", "In-memory seeded mock data (demo mode)"]
     other: ["Google Fonts (CDN)", "cdnjs (React/ReactDOM/Babel CDN delivery)"]

  what_we_are_most_proud_of: >         # Optional but recommended — guides judges to your best work
    "The explainability layer: every readiness flag is backed by a plain-language
    reason (current reading, % of threshold, trend rate, days-to-breach relative
    to the next mission window) rather than a black-box score, so a maintenance
    officer can trust and act on the recommendation immediately. The app also
    degrades gracefully — it runs fully on rule-based logic and mock data out of
    the box, and upgrades automatically to live watsonx.ai explanations and
    Db2/Cloudant data once credentials are supplied, without any code changes."

  known_limitations: >                 # Optional but recommended — be honest about gaps
   " Sensor and service-record data is currently synthetic (seeded mock data) —
    real HUMS/Db2 integration is scaffolded (see src/) but not populated with
    live data. The watsonx.ai and Db2/Cloudant integrations are implemented and
    toggle-ready in code but were not tested against live IBM Cloud credentials
    in this environment. Failure prediction uses a linear trend-rate projection
    rather than a trained ML model."


# =============================================================================
# ARTIFACT LOCATIONS
# These paths are relative to the repo root. Only change if you moved files.
# =============================================================================
artifacts:
  source_code: "src/"
  setup_guide: "docs/setup-guide.md"
  architecture_doc: "docs/architecture.md"
  demo_video: "demo/demo-video-link.txt"   # URL to hosted video (YouTube, Loom, Box)
  live_demo: "demo/live-demo-url.txt"      # URL to deployed demo (optional)
  screenshots: "demo/screenshots/"
  presentation: "presentation/"
